# CHECKLIST: Authoritative Networking — Testing

**Created**: 2026-06-02 | **Feature**: [spec.md](../spec.md)
**Domain**: testing | **Depth**: Standard | **Audience**: Reviewer (PR)
**Focus Areas**: cross-end determinism testability; reconciliation convergence after forced mismatch; interpolation under simulated loss/jitter; input-validation rejection; loopback equivalence; bandwidth-baseline repeatability; headless bot harness coverage

## Determinism Testability (TR-016 / SC-007)

- [ ] CHK001 Is the "reconciliation tolerance" within which server state and client prediction must agree (TR-016, SC-007) defined as a concrete, asserted epsilon a test can check rather than a qualitative "agree"? [Measurability, Spec §TR-016] <!-- Evaluator: ASK — observable form resolved (deterministic harness epsilon = 0 bit-identical, TR-016/032/034; live convergence epsilon `RECON_EPS` in TR-033). The concrete numeric `RECON_EPS` value is a play-feel product call captured as OD-001 (recommended default 0.05 m / 0.05 m·s⁻¹) pending user confirmation. -->
- [X] CHK002 Is the determinism comparison scoped to the deterministic in-memory loopback harness (identical code + `FixedDt`) versus a live cross-machine run, so the verification environment is unambiguous? [Clarity, Plan §Testing Strategy] <!-- Evaluator: Resolved — added TR-032 (two-environment scope: loopback bit-identical vs live manual) and plan §Testing Strategy "Determinism scope" note. -->
- [X] CHK003 Are the required inputs to a determinism test specified — identical numbered `ClientInput` streams fed to both the server `sim` and the client predicted `sim` from a known seed/initial state? [Completeness, Spec §TR-016] <!-- Evaluator: Resolved — added TR-034 (fixed seed + initial state + identical numbered input stream + fixed tick count). -->
- [X] CHK004 Does the spec state whether the bit-identical determinism guarantee is inherited from the E001 like-target test or must be re-asserted at the E003 split, so coverage ownership is clear? [Coverage, Spec §Risks(Cross-machine determinism)] <!-- Evaluator: Resolved — TR-032 states the primitive is owned by the E001 like-target test and re-exercised at the E003 split by TR-034; plan §Testing Strategy mirrors. -->
- [X] CHK005 Is the observable pass/fail signal for "determinism holds across the split" (SC-007) stated as a measurable state-divergence assertion rather than absence of visible desync? [Measurability, Spec §SC-007] <!-- Evaluator: Resolved — tightened SC-007 to a bit-identical (epsilon = 0) state-equality assertion via TR-034, not "absence of visible desync". -->

## Reconciliation Convergence (TR-009 / SC-002)

- [X] CHK006 Is "reconciles cleanly" (SC-002) given an observable convergence criterion (predicted state reaches authoritative within epsilon over N ticks) plus a no-teleport/no-snap criterion that a test can assert? [Measurability, Spec §SC-002] <!-- Evaluator: Resolved — added TR-033 (convergence bound within `RECON_EPS` over ≤5 snapshots + no-teleport `MAX_SNAP` per-tick bound) and tightened SC-002; numeric constants are OD-001 (CHK001). -->
- [X] CHK007 Is the method for producing a "forced prediction mismatch" (SC-002, TR-009) specified — e.g., an injected divergence or a scripted inter-player interaction — so the test condition is reproducible? [Clarity, Spec §SC-002] <!-- Evaluator: Resolved — added TR-035 (reproducible deterministic injection: scripted state override or server-resolved ram, fixed seed/inputs). -->
- [X] CHK008 Is the smoothed-correction bound that distinguishes "non-teleporting" from a hard snap defined (max per-tick correction or blend duration) so "smoothed" is testable? [Ambiguity, Spec §TR-009] <!-- Evaluator: Resolved — TR-033 defines the no-teleport bound (`MAX_SNAP` per-tick cap, blend across ≤5 ticks); recommended cap in OD-001. -->
- [X] CHK009 Is the replay requirement testable — that exactly the inputs with `seq > acked_input_seq` are re-applied after re-seeding (TR-009, contract reconciliation anchor)? [Coverage, Contract §Reconciliation anchor] <!-- Evaluator: Covered by contracts/protocol.md §Reconciliation anchor ("replays all buffered inputs with seq > acked_input_seq") + spec §TR-009. -->
- [X] CHK010 Is the convergence window (number of ticks/snapshots within which the client must converge) bounded so a non-converging or oscillating reconciliation is a definite test failure? [Edge-Case, Spec §SC-002] <!-- Evaluator: Resolved — TR-033 bounds the window at ≤5 snapshots and requires non-increasing residual (non-converging/oscillating = failure); SC-002 mirrors. -->
- [X] CHK011 Does the reconciliation requirement specify expected behavior for the high-likelihood inter-player misprediction case (e.g., a ram), so the "accept visible-but-graceful" boundary is an explicit acceptance, not unspecified? [Completeness, Spec §Risks(misprediction)] <!-- Evaluator: Covered by spec §Risks(Inter-player interaction misprediction) "accept visible-but-graceful reconciliation"; the ram case is now also the scripted mismatch source in TR-035. -->

## Interpolation Under Loss/Jitter (TR-010 / SC-004)

- [X] CHK012 Are the simulated loss and jitter test conditions quantified (drop rate, jitter distribution/magnitude, single vs consecutive drops) rather than just "simulated packet loss/jitter"? [Measurability, Spec §SC-004] <!-- Evaluator: Resolved — added TR-036 (5% uniform single-packet loss, ±50 ms uniform jitter, plus a scripted consecutive-drop case); SC-004 mirrors. -->
- [X] CHK013 Is "remote motion stays smooth" (SC-004) given an objective signal — no teleport beyond a position-delta threshold within the interpolation window — instead of a visual judgment? [Ambiguity, Spec §SC-004] <!-- Evaluator: Resolved — TR-036 defines the no-jump objective signal (`MAX_INTERP_DELTA` per rendered frame); SC-004 mirrors; constant in OD-001. -->
- [X] CHK014 Is the boundary of what the ~100 ms buffer must ride out specified (e.g., a single dropped snapshot vs how many consecutive drops before extrapolation/stall is acceptable)? [Edge-Case, Spec §Edge Cases] <!-- Evaluator: Resolved — TR-036 specifies single-drop = no visible jump, and the consecutive-drop case bounds how many drops the buffer absorbs before a stall/extrapolation is acceptable. -->
- [X] CHK015 Does the interpolation test requirement state the snapshot send rate (15–20 Hz) and interpolation delay (~100 ms) as fixed test parameters so the buffer-window assertion is deterministic? [Clarity, Spec §TR-010] <!-- Evaluator: Resolved — TR-036 fixes the send rate (baseline 20 Hz) and interpolation delay (~100 ms) as fixed test parameters. -->
- [X] CHK016 Is the late/duplicate/out-of-order packet handling (stale snapshots discarded by sequence) covered by a stated test condition, distinct from pure packet loss? [Coverage, Spec §Edge Cases] <!-- Evaluator: Resolved — added TR-037 (stale/dup/out-of-order snapshots discarded by `server_tick`, buffer advances monotonically), distinct from loss (TR-036) and input replay (TR-022/023); SC-012 mirrors. -->

## Input-Validation Rejection (TR-011 / TR-012 / SC-003)

- [X] CHK017 Does each invalid-input class in SC-003 (out-of-bounds thrust/turn, excessive fire rate, replayed sequence, asserted position/hit) have its own enumerated rejection test rather than one blanket "invalid input" case? [Completeness, Spec §SC-003] <!-- Evaluator: Resolved — added TR-038 (four enumerated per-class rejection cases: clamp analog / fire-rate gate / replay-stale discard / asserted position-hit ignored); SC-003 mirrors. -->
- [X] CHK018 Is "leaves the authoritative state unaffected" (SC-003) given a measurable assertion — server `sim` state identical before/after the rejected input — so rejection is observable? [Measurability, Spec §SC-003] <!-- Evaluator: Resolved — added TR-039 (byte-for-byte sim-state equality pre/post, except input-ack bookkeeping; clamp case asserts the clamped value); SC-003 mirrors. -->
- [X] CHK019 Are the thrust/turn physical bounds and the fire rate-limit threshold defined as concrete clamp/reject values so an out-of-bounds test case can be constructed? [Clarity, Spec §TR-011] <!-- Evaluator: Covered by spec §TR-020 (analog fields clamped to quantized −1..=1) + §TR-021 (fire rate bound = the existing E002 `sim` weapon cooldown). -->
- [X] CHK020 Is the stale/replayed-sequence rejection condition specified in terms the test can drive (sequence ≤ acked or older than a window) per the sequence/ack tracking? [Ambiguity, Spec §TR-011] <!-- Evaluator: Covered by spec §TR-022 (replay = `seq` ≤ last-processed; stale = `tick` older than the acceptance window TR-027) + §TR-023 (out-of-order), discard-only. -->
- [X] CHK021 Is the requirement that client-asserted positions/hits are ignored (TR-012) testable as a positive assertion that only server-`sim`-derived motion and server-resolved hits appear in the snapshot? [Coverage, Spec §TR-012] <!-- Evaluator: Covered by spec §TR-012 + SC-003 VC2 (only server-`sim`-derived state and server-resolved hits count); the positive assertion is now part of the TR-038 case (4) with the TR-039 equality check. -->
- [X] CHK022 Is the baseline lag-compensated hit-resolution (rewind to firer's viewed time, TR-012) given a verifiable acceptance, or explicitly scoped out of E003 test depth as "baseline only"? [Edge-Case, Spec §TR-012] <!-- Evaluator: Covered by spec §TR-017 (rewind = interp delay + RTT, capped 500 ms, defined out-of-window fallback; "full WAN tuning deferred" = baseline-only). -->

## Loopback Equivalence (TR-003 / SC-008)

- [X] CHK023 Is "behaviorally equivalent to a networked connection" (SC-008) defined as a specific set of paths that must match (authoritative-state, prediction, interpolation) so equivalence is checkable rather than asserted? [Measurability, Spec §SC-008] <!-- Evaluator: Resolved — added TR-040 (four named must-match paths: authoritative-state delivery, prediction, reconciliation, interpolation); SC-008 mirrors. -->
- [X] CHK024 Does the spec state which behaviors are allowed to differ between loopback and real UDP (e.g., latency/loss) so the equivalence test does not falsely fail on transport-only differences? [Consistency, Spec §SC-008] <!-- Evaluator: Resolved — TR-040 names transport-only allowed differences (latency, loss, jitter, MTU framing) and requires matched loss-free/zero-latency comparison; SC-008 mirrors. -->
- [X] CHK025 Is it specified that the renet UDP adapter is exercised by a separate test on the loopback address, distinct from the in-memory `NetTransport`, so both transport paths have stated coverage? [Coverage, Plan §Testing Strategy] <!-- Evaluator: Resolved — added TR-041 (separate UDP-adapter test bound to `127.0.0.1`, distinct from the in-memory transport) and plan §Testing Strategy Integration row now states it. -->

## Bandwidth-Baseline Repeatability (TR-014 / SC-005)

- [X] CHK026 Is the bandwidth-baseline measurement (bytes/client/sec, SC-005) defined with a fixed, repeatable test scenario — entity count, session duration, client/bot count — so the recorded figure is reproducible? [Measurability, Spec §SC-005] <!-- Evaluator: Resolved — added TR-042 (fixed session: 2 bot clients + 4 bot ships, fixed scripted inputs, 30 s window, 20 Hz, fixed seed); SC-005 mirrors. -->
- [X] CHK027 Is the measurement source specified (e.g., `NetTransport::stats` bytes in/out) and the metric unit/aggregation window stated so two runs are comparable? [Clarity, Contract §Transport adapter trait] <!-- Evaluator: Resolved — TR-042 names `NetTransport::stats` bytes-out as the source and mean bytes/client/sec over the 30 s window (plus peak) as the metric/aggregation. -->
- [X] CHK028 Does the baseline requirement state whether it is recorded-only or has a pass/fail threshold, so reviewers know it is a measurement and not yet a budget gate (E009)? [Consistency, Spec §Scope(Excluded)] <!-- Evaluator: Resolved — TR-042 + SC-005 now state explicitly recorded-only with no pass/fail budget gate (budget deferred to E009), consistent with spec §Scope(Excluded). -->

## Headless Bot Harness Coverage (TR-015)

- [X] CHK029 Does TR-015 enumerate the scenarios the harness must exercise — prediction responsiveness, forced-mismatch reconciliation, invalid-input rejection, smooth interpolation under loss, bytes/client/sec baseline — so harness coverage is complete and traceable? [Coverage, Spec §TR-015] <!-- Evaluator: Resolved — added TR-043 enumerating the six harness scenarios with SC/TR traces; plan §Testing Strategy Integration row mirrors. -->
- [X] CHK030 Is the harness required to drive ≥2 networked clients headlessly (no rendering) as a stated precondition, so "smooth interpolation" assertions use a non-visual signal? [Clarity, Spec §TR-015] <!-- Evaluator: Resolved — TR-043 requires ≥2 networked clients headless/no rendering and non-visual numeric signals (state deltas, seq/ack, stats). -->
- [X] CHK031 Does each P1 success criterion (SC-001..SC-004, SC-007, SC-008) trace to at least one harness or unit test tier, so no P1 acceptance is left without an automated check? [Traceability, Plan §Testing Strategy] <!-- Evaluator: Resolved — TR-043 + plan §Testing Strategy map every P1 SC to a tier (SC-001/002/003/004 → harness scenarios; SC-007 → TR-034 unit/loopback determinism; SC-008 → TR-040 equivalence test). -->
- [X] CHK032 Is the client-disconnect-mid-session edge case (server drops session cleanly, remaining clients continue) given a stated test scenario in the harness coverage? [Edge-Case, Spec §Edge Cases] <!-- Evaluator: Resolved — TR-043 scenario (6) is the client-disconnect-mid-session clean-slot case (server frees only that slot, remaining clients continue, TR-031). -->
- [X] CHK033 Is the protocol message round-trip (encode/decode equality, OBJ2 VC-1) covered by a stated unit test independent of the bot harness? [Coverage, Spec §OBJ2] <!-- Evaluator: Resolved — TR-043 requires the OBJ2 VC-1 round-trip as a unit test independent of the bot harness; plan §Testing Strategy Unit tier now lists it explicitly. -->
