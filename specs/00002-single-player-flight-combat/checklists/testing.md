# Testing Requirements Checklist: Single-player Flight & Combat

**Created**: 2026-06-01 | **Feature**: [spec.md](../spec.md)
**Domain**: testing | **Depth**: Standard | **Audience**: Reviewer (PR)
**Focus Areas**: Fixed-step determinism & frame-rate independence; Combat/AI acceptance (health/destroy, ram threshold, drifting-lead, seek-AI); Feel-gate measurability (SC-008 "feels good" + flight-assist observability); Swept-CCD edge cases (high-velocity, grazing, thin-target, simultaneous-hit, no-tunneling)

## Fixed-step Determinism & Frame-rate Independence

- [X] CHK001 Is the fixed simulation tick rate (default 60 Hz logical) stated as a requirement value that tests can assert against, rather than only as a plan note? [Measurability, Plan §Technical Context / FR-004] <!-- Evaluator: Resolved — added FR-016 (fixed 60 Hz logical tick as a defined assertable value) to spec.md -->
- [X] CHK002 Does FR-004 define "frame-rate independent" with an observable equivalence criterion (e.g., identical sim trajectory across frame rates) instead of the subjective "smooth"? [Ambiguity, Spec §FR-004] <!-- Evaluator: Resolved — amended FR-004 to define frame-rate independence as the per-tick sim-trajectory equivalence criterion (ref FR-017), replacing reliance on "smooth" -->
- [X] CHK003 Is the set of render frame rates to validate (30/60/144 FPS) specified as a normative test condition, and is "consistent feel" given a measurable tolerance? [Measurability, Spec §SC-001] <!-- Resolved (user decision 2026-06-01): amended FR-017 + SC-001 — the automated cross-FPS check asserts bit-identical sim state at equal tick counts under a deterministic harness (zero tolerance); live cross-FPS "consistent feel" is validated via the SC-008 manual gate (no numeric epsilon, by design). -->
- [X] CHK004 Are the determinism guarantees (what must be reproducible: same inputs + same start state → same state) defined as a requirement, given the integrator↔analytic invariant is "covered-by-E001"? [Completeness, Plan §Testing Strategy / FR-002] <!-- Evaluator: Resolved — added FR-017 (same start state + same per-tick inputs + same tick count → same sim state; extends E001 motion invariant to this slice's gameplay systems) to spec.md -->
- [X] CHK005 Is the "no spiral of death" stability boundary defined with a testable condition (e.g., max catch-up steps or accumulator cap) rather than a narrative edge case? [Measurability, Spec §Edge Cases] <!-- Evaluator: Covered by data-model.md §Invariants INV-11 (accumulator clamped → bounded catch-up, no spiral-of-death) + FixedStepClock resource (clamp accumulator) -->
- [X] CHK006 Is the rendering-interpolation requirement (interpolate between two latest sim states by alpha/overstep) expressed so a test can distinguish interpolated render from raw-state render? [Clarity, Spec §FR-004 / Plan §AD-001] <!-- Evaluator: Covered by data-model.md §New Components (RenderInterp: prev/curr_pos + alpha=accumulator/dt lerp) + INV-11; Plan AD-001 (overstep_fraction) -->
- [X] CHK007 Does the spec or plan state which test tier owns fixed-step determinism (headless `Time<Fixed>` integration per Plan §Testing Strategy) so coverage is unambiguous? [Traceability, Plan §Testing Strategy] <!-- Evaluator: Covered by plan.md §Testing Strategy Integration row (headless bevy_ecs / manually-advanced Time<Fixed> owns fixed-step determinism) -->
- [X] CHK008 Is frame-rate-spike behavior (physics stays stable while rendering interpolates) traceable from the edge case to a concrete acceptance condition rather than only prose? [Coverage, Spec §Edge Cases] <!-- Evaluator: Covered by data-model.md INV-11 (clamped accumulator + alpha∈[0,1]) tying the spec frame-rate-spike Edge Case to a concrete invariant, owned by the Testing Strategy Integration tier -->

## Combat & AI Acceptance

- [X] CHK009 Is the target health/damage/destroy lifecycle defined with enough precision (damage applied on every hit, destroy at Health≤0, removal) to author pass/fail assertions? [Measurability, Spec §FR-007 / SC-004] <!-- Evaluator: Covered by data-model.md §State Machines Target lifecycle (Health -= Damage; Health<=0 → despawn) + INV-01/INV-08; Spec FR-007/SC-004 -->
- [X] CHK010 Is the lethal ram threshold specified as a single tunable comparison (at/above destroys, below bounces) with the boundary condition at exactly the threshold defined? [Edge-Case, Spec §FR-010 / SC-005] <!-- Evaluator: Covered by FR-010 (at/above destroys, below bounces) + data-model.md Ship-ram state machine (closing speed >= lethal_ram_speed → destroyed); boundary is inclusive at threshold -->
- [X] CHK011 Is "believable momentum-transferring bounce" given an observable acceptance signal (e.g., both bodies' post-collision velocities reflect momentum transfer) rather than left subjective? [Ambiguity, Spec §SC-005] <!-- Evaluator: Resolved — amended SC-005 to add the observable signal (post-collision velocities consistent with closed-form elastic 2-body impulse; total linear momentum conserved; bodies separate) -->
- [X] CHK012 Is the drifting-target leading requirement defined with a testable outcome (a correctly-led shot at a constant-velocity asteroid connects) independent of player skill? [Measurability, Spec §US2 / FR-008] <!-- Evaluator: Covered by US2 Acceptance Scenario 4 (correctly-led shot at a drifting asteroid connects) + FR-008 -->
- [X] CHK013 Does the seeking-AI acceptance specify the observable behavior to assert (thrusts toward player position, can be led and destroyed) distinct from any internal steering algorithm? [Clarity, Spec §FR-012 / SC-007] <!-- Evaluator: Covered by FR-012 + SC-007 + US5 AS1/AS2 (thrust direction toward player position; led + destroyed) + data-model Seeker; observable behaviour stated independent of steering algorithm -->
- [X] CHK014 Is the "destroyable like other targets" claim for the seeker traceable to the shared health/destroy requirement so it is not re-specified inconsistently? [Consistency, Spec §FR-007 / FR-012] <!-- Evaluator: Covered by data-model.md (Seeker is a TargetKind; Health on every Target; shared Target destroy lifecycle) tracing FR-012 "destroyable like other targets" to FR-007 -->
- [X] CHK015 Are the three target subtypes (dummy, asteroid, seeker) each covered by acceptance requirements, and is asteroid's dual role (target + ship collider) reconciled between FR-008 and FR-009? [Consistency, Spec §FR-008 / FR-009] <!-- Evaluator: Covered by data-model.md TargetKind enum + entity-composition table (all three subtypes) and explicit asteroid dual role (target via FR-008 + ship collider via FR-009) -->
- [X] CHK016 Is hit/destroy feedback (visual/audio) defined with an observable signal a test can detect, rather than only "clear feedback"? [Measurability, Spec §FR-007 / SC-004] <!-- Evaluator: Covered by data-model.md Target lifecycle — the test-detectable signal is damage-applied (INV-08) + exactly-once despawn (INV-09); the visual/audio layer is the SC-008 manual gate per the Plan subjective-vs-automatable boundary -->
- [X] CHK017 Is the "projectile resolves harmlessly when its target is destroyed mid-flight" edge case stated as a verifiable post-condition (no error, no phantom damage)? [Edge-Case, Spec §Edge Cases] <!-- Evaluator: Covered by data-model.md Projectile lifecycle (target despawns mid-flight → projectile continues and later expires harmlessly, no effect) -->

## Feel-gate Measurability & Flight-assist Observability

- [X] CHK018 Is the SC-008 "feels good" gate defined with a repeatable evaluation procedure (named loop: pilot→aim→fire→hit→destroy, in both assist modes) so a reviewer can confirm the gate is runnable? [Measurability, Spec §SC-008] <!-- Evaluator: Covered by SC-008 (named pilot→aim→fire→hit→destroy loop, run in both assist modes) + research.md §vertical-slice; procedure is defined (intentionally manual per slice scope) -->
- [X] CHK019 Does SC-008 specify the pass/fail signal (evaluator rates feel positive; negative findings logged) clearly enough to distinguish a pass from a fail? [Clarity, Spec §SC-008] <!-- Evaluator: Covered by SC-008 — pass = evaluator rates flight/combat feel positive; negative findings are logged for tuning; the signal distinguishes pass from fail -->
- [X] CHK020 Is it explicit which feel aspects are validated by the manual playtest versus automatable tests, so the boundary between subjective and asserted coverage is unambiguous? [Coverage, Plan §Testing Strategy / SC-008] <!-- Evaluator: Resolved — added a "Subjective vs automatable boundary" paragraph to plan.md §Testing Strategy enumerating which aspects are manual (SC-008/SC-006) vs asserted by automated tests -->
- [X] CHK021 Is flight-assist mode observability defined so each mode's effect is testable: assist OFF preserves the velocity vector under rotation, assist ON trends velocity toward heading? [Measurability, Spec §FR-003 / SC-002] <!-- Evaluator: Covered by FR-003 + SC-002 + US1 AS2 (OFF: heading changes, velocity unchanged) / AS3 (ON: velocity trends toward heading) + data-model FlightAssist state machine -->
- [X] CHK022 Is the assist-toggle mid-maneuver transition requirement ("no instantaneous velocity snap") expressed as a measurable continuity condition rather than a feel adjective? [Ambiguity, Spec §Edge Cases / FR-003] <!-- Evaluator: Covered by data-model.md INV-07 + FlightAssist continuity invariant (toggle never mutates Velocity on the transition frame) — a measurable condition, not an adjective -->
- [X] CHK023 Is the HUD readability requirement (speed/throttle, assist mode, reticle, hit/destroy, "without number spam") defined with observable acceptance criteria a reviewer can check? [Measurability, Spec §FR-011 / SC-006] <!-- Evaluator: Covered by FR-011 + SC-006 + US4 acceptance scenarios — concrete enumerated HUD elements (speed/throttle, assist mode, reticle, hit/destroy update) a reviewer can check; "no number spam" is a reviewer readability check per slice's manual-feel posture -->
- [X] CHK024 Are the in-engine tunable magnitudes (thrust, max speed, projectile speed, lethal ram threshold) required to be observable/inspectable so feel-tuning iterations are testable? [Traceability, Spec §FR-015 / Plan §Data Model (Tuning)] <!-- Evaluator: Covered by FR-015 + data-model.md Tuning resource (centralised tunable magnitudes) + INV-10 range validation; tests set Tuning and assert behaviour -->
- [X] CHK025 Is the coasting requirement (constant velocity, no further input) stated as a verifiable steady-state condition usable as an acceptance assertion? [Measurability, Spec §US1 / FR-002] <!-- Evaluator: Covered by US1 Acceptance Scenario 1 (accelerate then coast at constant velocity without further input) + FR-002 + Glossary "Coasting" -->

## Swept-CCD Edge Cases

- [X] CHK026 Does FR-006 define "no tunneling" across the full projectile-velocity range as a testable property, including the high relative closing-speed case? [Edge-Case, Spec §FR-006 / SC-003] <!-- Evaluator: Covered by FR-006 + SC-003 (hits across full velocity range, no tunneling) + US2 AS2 (path crosses target between frames → hit) + data-model INV-08 -->
- [X] CHK027 Is the grazing/tangent hit required to resolve to a single defined hit-or-miss outcome (no flicker) with the tie-breaking rule specified enough to test deterministically? [Ambiguity, Spec §Edge Cases / SC-003] <!-- Evaluator: Resolved — amended spec §Edge Cases grazing/tangent bullet with the tie-break rule (closest-approach distance <= CollisionRadius counts as a hit; pure deterministic geometry → no flicker) -->
- [X] CHK028 Is the thin/small-target case bounded with a concrete minimum target size or speed cap (per the velocity-cap + inflated-hitbox mitigation) so the no-tunneling test has defined parameters? [Edge-Case, Spec §Edge Cases / Plan §Risk Mitigation] <!-- Evaluator: Resolved — amended spec §Edge Cases thin-target bullet to parameterise the no-tunneling test off Tuning (speed up to Tuning.muzzle_speed cap; radius >= inflated minimum), with concrete values in-engine tunable (FR-015) -->
- [X] CHK029 Is the simultaneous multi-hit case (two projectiles, one target, one frame) specified with a defined damage-application order/outcome and no double-destroy artifact? [Edge-Case, Spec §Edge Cases / SC-003] <!-- Evaluator: Covered by data-model.md INV-09 + Target lifecycle (multi-hit damage accumulates order-independently; despawn exactly once even on overkill) -->
- [X] CHK030 Are the swept-CCD edge cases (high-velocity, grazing, thin-target, simultaneous) each traceable to a named unit/integration test owner in the Testing Strategy rather than only listed in the spec? [Traceability, Plan §Testing Strategy / Risk Mitigation] <!-- Evaluator: Resolved — amended plan §Risk Mitigation swept row to enumerate all four edge cases (incl. thin/small-target) as owned by the swept segment-vs-circle unit tests in the Testing Strategy Unit tier -->
- [X] CHK031 Is the `Physics`-trait swap-equivalence requirement (Stub vs Rapier swept-cast/contact produce equivalent results) defined as a testable invariant for the extended trait? [Consistency, Plan §Testing Strategy / §AD-002] <!-- Evaluator: Covered by plan.md §Testing Strategy (extended Physics swap-equivalence) + AD-002 + HINT-001 + Project Structure tests/physics_swap.rs (swept-cast/contact swap-equivalence cases) -->
