# Testing Requirements Checklist: Workspace & Sim Core
**Created**: 2026-06-01 | **Feature**: [spec.md](../spec.md)

## Test-Strategy Completeness

- [X] CHK001 Is each testing tier in the strategy table (Unit, Integration, Security, Coverage) tied to a specific requirement or success criterion it validates? [Traceability, plan.md §Testing Strategy] <!-- Evaluator: Resolved — added a "Validates" column mapping every tier to TR-###/SC-### in plan.md §Testing Strategy -->
- [X] CHK002 Does the Unit tier scope name every motion invariant it must exercise (equivalence, zero-accel coasting, negative control, serde round-trip), or is "component/serde round-trip" left to interpretation? [Completeness, plan.md §Testing Strategy] <!-- Evaluator: Covered by plan.md §Testing Strategy (Unit row names equivalence, zero-accel coasting, zero-dt no-op, forward-Euler negative control, component/serde round-trip) -->
- [X] CHK003 Is the mock boundary for the Integration tier ("physics backend stubbed") specified clearly enough to know what the stub must implement to be a valid `Physics` substitute? [Clarity, TR-006] <!-- Evaluator: Resolved — Mock Boundary cell now states the stub MUST implement the full Physics trait surface using only glam/sim types (plan.md §Testing Strategy) -->
- [X] CHK004 Are the Security (`cargo-audit`) and Coverage (`cargo-llvm-cov`) tiers stated as gated or non-gated, and is the non-gating decision for coverage justified against the "motion invariants MUST be covered" rule? [Consistency, plan.md §Testing Strategy] <!-- Evaluator: Resolved — added a "Gated?" column; coverage non-gating justified by the invariants being enforced as failing Unit tests rather than a coverage % (plan.md §Testing Strategy) -->
- [X] CHK005 Does any requirement or strategy entry define how a failing `cargo-audit` advisory is triaged, given no severity threshold or owner is stated? [Completeness, plan.md §Testing Strategy] <!-- Evaluator: Resolved — added TR-010 (gate high/critical, track low/medium, sim::physics owner) to spec.md and the Security row + .cargo/audit.toml to plan.md -->
- [X] CHK006 Is the per-step allocation-free expectation (Performance Goals) expressed as a testable assertion, or only as guidance with no tier that checks it? [Testability, plan.md §Technical Context] <!-- Evaluator: Resolved — Performance Goals now state it is non-gated design guidance (satisfied by Copy value types), with a benchmark gate deferred to E003/E008 (plan.md §Technical Context) -->


## Success-Criteria Measurability & Coverage

- [X] CHK007 Is the "documented tolerance" in SC-002/TR-004 quantified (a concrete numeric bound) anywhere, or referenced as undefined and therefore unverifiable? [Measurability, SC-002] <!-- Evaluator: Resolved — quantified as rel_tol = 1e-4 (primary) / 2e-4 (across tick sizes) with the relative-bound formula in TR-004, SC-002, and Objective 2 (per motion.rs keystone) -->
- [X] CHK008 Are the "representative tick rates" pinned to specific values (10–144 Hz endpoints plus intermediate rates), or is the sampling set left ambiguous? [Measurability, SC-002] <!-- Evaluator: Resolved — pinned to the set {10, 20, 30, 60, 144} Hz in TR-005, SC-002, and Objective 2 (per motion.rs keystone) -->
- [X] CHK009 Does every success criterion (SC-001…SC-004) map to at least one named test tier or check in the Testing Strategy? [Coverage, SC-001] <!-- Evaluator: Resolved — Testing Strategy "Validates" column maps SC-001/SC-003→Build/Lint/Format tier, SC-002→Unit, SC-004→Integration (plan.md §Testing Strategy) -->
- [X] CHK010 Is SC-001's "zero rendering/windowing/input/audio/networking libraries" requirement backed by a stated verification method (dependency-graph inspection), and is that inspection automatable in CI? [Testability, SC-001] <!-- Evaluator: Resolved — added TR-009 (cargo tree/cargo metadata dep-graph inspection in CI); SC-001 now cites it; Build/Lint/Format tier runs it (plan.md) -->
- [X] CHK011 Are SC-003's gate outcomes ("no findings", "green") defined as pass/fail conditions the harness can evaluate without human judgment? [Measurability, SC-003] <!-- Evaluator: Resolved — TR-007 + SC-003 now state clippy -D warnings fails on any warning, fmt --check fails on any unformatted file, green test requires all tests pass (machine-evaluable) -->
- [X] CHK012 Is SC-004's "no Rapier2D-specific types appear in consumer code" criterion expressed in a way that can be checked (e.g., trait-signature/type-leak audit) rather than by inspection alone? [Testability, SC-004] <!-- Evaluator: Resolved — TR-006 + SC-004 now define the type-leak audit (consumer signatures reference only glam/sim types) and the swap integration test guarding it -->
- [X] CHK013 Does each TR-001…TR-008 trace to a success criterion or a test in the coverage map, and are TR-007/TR-008 (gates, serde-friendliness) explicitly covered? [Traceability, TR-008] <!-- Evaluator: Covered by plan.md §Requirement Coverage Map (TR-001…TR-010 rows incl. TR-007 CI gates and TR-008 serde round-trip) -->


## Core Invariant & Edge-Case / Failure-Mode Coverage

- [X] CHK014 Does the equivalence requirement specify both the position AND velocity outputs must agree with the analytic evaluator, or only position? [Completeness, TR-004] <!-- Evaluator: Resolved — TR-004, SC-002, and Objective 2 now require both position AND velocity to agree (matches motion.rs keystone asserting both) -->
- [X] CHK015 Is the negative control's requirement precise about what "a deliberately incorrect integrator" is, so the test proves the assertion can actually fail? [Clarity, TR-005] <!-- Evaluator: Resolved — pinned the negative control to forward (explicit) Euler with absolute position error > 0.1 in TR-005 and Objective 2 (per motion.rs keystone) -->
- [X] CHK016 Does the equivalence coverage state how step count N (or elapsed `t = N·dt`) is chosen, so the test exercises accumulation over a meaningful horizon rather than a single step? [Coverage, SC-002] <!-- Evaluator: Resolved — TR-004 + Objective 2 pin the horizon at ≈100 s / 3,000 steps at 30 Hz (and 60 s across the rate set), per motion.rs keystone -->
- [X] CHK017 Is the long-run floating-point drift edge case (integrator diverging from closed form over time) backed by a requirement that bounds and tests drift, not just a documented note? [Coverage, spec §Edge Cases] <!-- Evaluator: Resolved — Edge Cases now ties drift to TR-004/SC-002 with a bounded rel_tol=1e-4 tested over the ≈100 s/3,000-step horizon -->
- [X] CHK018 Is the runtime-variable-timestep edge case ("same logical `dt` per tick, different wall-clock rate must not change the logical result") covered by a stated test obligation? [Coverage, spec §Edge Cases] <!-- Evaluator: Resolved — Edge Cases now states the TR-005 test obligation: equivalence must pass at every rate in {10,20,30,60,144} Hz, proving the logical result is tick-rate-invariant -->
- [X] CHK019 Are degenerate inputs (zero acceleration, zero `dt`, large coordinate magnitudes) each assigned an expected, testable behavior or an explicit out-of-scope deferral? [Completeness, spec §Edge Cases] <!-- Evaluator: Resolved — Edge Cases now assigns: zero-accel→straight-line coasting test, zero-dt→no-op step, large coords→explicit out-of-scope (sector-relative downstream) -->
- [X] CHK020 Is there a stated test obligation that no engine-specific Rapier2D type crosses the `Physics` trait boundary, covering the "swap guarantee is void" failure mode? [Coverage, TR-006] <!-- Evaluator: Resolved — TR-006 now mandates the integration swap test + type-leak audit (consumer signatures reference only glam/sim types) -->
- [X] CHK021 Does the serde round-trip requirement specify which domain types must survive a round-trip and what equality/equivalence defines success? [Measurability, TR-008] <!-- Evaluator: Resolved — TR-008 now names BodyState + gameplay components and defines success as deserialized == original under PartialEq -->
- [X] CHK022 Does the `Physics`-trait-swap integration test requirement define "identical consumer behavior" concretely enough to assert (same outputs for the same inputs across both implementations)? [Clarity, SC-004] <!-- Evaluator: Resolved — TR-006, SC-004, and plan.md Integration row define "identical consumer behavior" as same outputs for the same inputs under both implementations -->
- [X] CHK023 Are the cross-machine bit-determinism exclusion and its consequence (consistent `f32` + fixed `dt` is sufficient) stated so tests do not erroneously assert determinism that is out of scope? [Consistency, spec §Technical Constraints] <!-- Evaluator: Covered by spec.md §Technical Constraints (bit-determinism NOT required; consistent f32 + fixed dt + one code path sufficient) and plan.md HINT-002 -->

