# Requirements Quality Checklist: Ship AI Architecture — Observability

**Created**: 2026-06-10 | **Feature**: [spec.md](../spec.md)
**Domain**: observability | **Depth**: Standard | **Audience**: Author (pre-implementation)
**Focus Areas**: utility score-breakdown debug view (AD-006); AI runtime metrics (per-tier think time, squad/aggregate counts, promotion/demotion rates, perception scan costs, off-screen battle count); AiTuning live-editing feedback; diagnosing AI misbehavior + determinism drift (signals/replay); dev-panel integration; SAD observability baseline (tracing + Prometheus + replay recorder) as context

## Score-Breakdown Debug View (AD-006)

- [X] CHK001 Is the content of the score-breakdown view specified — which elements it must show (per-candidate-behavior considerations, raw vs curve-transformed values, the multiplied product, the ~25% incumbent momentum bonus, priority bucket, the winning score) — or only that a "view" exists? [Completeness, plan §AD-006 + research §Utility-FSM] <!-- Evaluator: Resolved — added TR-020(a) to spec.md: per-candidate consideration values, curve-multiplied score, momentum bonus, priority bucket, winning score -->
- [X] CHK002 Is the ship-selection mechanism for "per-ship" defined (selected entity, hovered, nearest to camera, entity-id entry), so the view is buildable without inventing UX? [Clarity, plan §AD-006 / §Project Structure dev_panel.rs] <!-- Evaluator: Resolved — TR-020(a): list of AI ships sorted by distance to the player ship, default = nearest, stable-id entry override -->
- [X] CHK003 Do the requirements state which decision layers the breakdown covers — individual brains only, or also squad/wing-brain order selection and dormant-aggregate decisions — given decision-making moves up the hierarchy at mid/far tiers? [Coverage, plan §AD-006 vs Spec §OBJ3/TR-009] <!-- Evaluator: Resolved — TR-020(a): individual brains; squad-driven ships show the squad/wing brain's order decision; dormant aggregates show tier + glide state -->
- [X] CHK004 Is it specified whether the view shows the scores from the last actual think (event-driven — possibly many ticks stale) or a live recompute, and whether staleness (`last_think_tick`, `commit_until_tick`) must be displayed? [Ambiguity, Spec §TR-005 / data-model §AiBrain] <!-- Evaluator: Resolved — TR-020(a): last-actual-think capture, never a live recompute; staleness shown via last_think_tick + commit_until_tick -->
- [X] CHK005 Is there a requirement that the debug view be strictly read-only with zero effect on sim state, decision order, or determinism (no extra thinks triggered by viewing)? [Completeness, Spec §TR-004/TR-016] <!-- Evaluator: Resolved — TR-020: strictly read-only; viewing never triggers thinks or perturbs sim state/decision order/determinism -->
- [X] CHK006 Is AD-006's availability reconciled across deployment targets — the dev panel is client-side but brains run in the shared sim (client-embedded loopback vs headless server) — is loopback-only acceptable and stated? [Consistency, plan §Project Structure vs §Technical Context Target Platform] <!-- Evaluator: Resolved — TR-020: client-embedded loopback ONLY, direct ECS reads of the embedded server world; matches the existing dev-panel solo/server-side pattern -->
- [X] CHK007 Does AD-006 have any acceptance/validation criterion (no TR or SC covers the score-breakdown view), so QC can assert it was delivered rather than treating it as optional? [Traceability, plan §AD-006 — gap] <!-- Evaluator: Resolved — added TR-020 to spec.md Requirements + a TR-020 coverage-map row in plan.md (client dev_panel + sim-side gated score-capture seam) -->

## AI Runtime Metrics

- [X] CHK008 Are per-tier think-time metrics (active / mid / dormant, plus steering and perception phases) required as distinct observable signals, so a ≤30% budget breach can be attributed to a subsystem rather than discovered as an aggregate? [Completeness, Spec §TR-017/SC-003] <!-- Evaluator: Resolved — TR-020(c) requires per-tier ship counts + per-tier think time at runtime; TR-018 already attributes bench cost per bucket (think/steering/perception/squad/LOD/coarse) -->
- [X] CHK009 Do the requirements demand runtime counts of brains per tier, live squads/wings, and dormant aggregates as observable metrics during play (not only inside the `fleet_stress` bench)? [Completeness, Spec §SC-003 / plan §Architecture ai::lod] <!-- Evaluator: Resolved — TR-020(c): per-tier ship counts + live squad/wing and dormant-aggregate counts in the dev-panel metrics readout -->
- [X] CHK010 Are promotion/demotion rates (tier transitions per unit time) required as a metric, given hysteresis thrash is a named pitfall and `since_tick` exists specifically to prevent it? [Coverage, research §AI LOD Pitfalls / data-model §AoiTier] <!-- Evaluator: Resolved — TR-020(c): tier promotion + demotion rates required; data-model §AoiTier defines the thrash interpretation -->
- [X] CHK011 Is perception scan cost required to be observable (scans per tick by tier, fused scans per squad vs per-ship scans, contacts produced), so the tier-scaled cadence claims of TR-013 can be confirmed at runtime? [Completeness, Spec §TR-013 / Clarification Q4] <!-- Evaluator: Resolved — TR-020(c): perception scan counts/costs per tier + fused-contact totals -->
- [X] CHK012 STF-001 defers mitigation "until that load is real" — do the requirements demand the signal that detects this condition (a count of concurrently live off-screen promoted battles and their tick cost), without which the revisit trigger is unobservable? [Traceability, Spec §STF-001 / plan §Complexity Tracking] <!-- Evaluator: Covered by spec TR-018 (off-screen promoted-battle bucket measured + reported separately, machine-readable) + STF-001's >10%-of-tick-budget revisit trigger; TR-020(c) adds the live battle count -->
- [X] CHK013 Is the `fleet_stress --ai` report format specified (per-system time breakdown vs single overhead %, baseline-comparison protocol, run duration, N) precisely enough that two runs are comparable and the recorded scale target is reproducible? [Measurability, Spec §TR-017 / plan §IP-008] <!-- Evaluator: Covered by spec TR-018 + plan §TR-017/TR-018 Bench Protocol — pinned R57 config, N=2000, ≥30 warmup + ≥120-tick window, paired runs, per-bucket means + p50/p99, machine-readable report + exit code -->
- [X] CHK014 Is "≤ ~30% overhead at sustainable N" measurable as written — which statistic (mean/p99 per-tick time), over how many ticks, at which N within the ~2000–4000 range — and is the tolerance implied by "~" bounded? [Ambiguity, Spec §TR-017/SC-003 / Clarification Q5] <!-- Evaluator: Covered by spec TR-017/TR-018 — hard gate (>30.0% fails), mean-tick statistic over a ≥120-tick window at pinned N=2000, p99 33.3 ms backstop; the "~" was removed from the gate -->
- [X] CHK015 Are event-scheduler effectiveness signals required (event-triggered thinks vs fallback-cadence thinks vs skipped ticks), so the "calm ships incur ≈0 decision cost" claim is verifiable outside a profiler? [Measurability, Spec §TR-005] <!-- Evaluator: Resolved — TR-020(c) adds think counts split event-triggered vs fallback-cadence; TR-018's calm-fleet case already verifies ≈0 idle think cost in the bench -->
- [X] CHK016 Do the requirements state whether AI metrics integrate with the SAD baseline pipeline (`tracing` + Prometheus, per-bubble tick time) or whether bench-output + dev-panel-only is the accepted scope for this single-process phase — and is that boundary explicit? [Consistency, SAD §Observability vs plan §Testing Strategy] <!-- Evaluator: Resolved — TR-020 states the boundary: v1 surfaces = dev panel + TR-018 bench report; the SAD tracing/Prometheus baseline applies when the headless server grows runtime metrics (later epic) -->

## Tuning Visibility (AiTuning)

- [X] CHK017 Is the set of `AiTuning` fields exposed as dev-panel sliders specified (all eight field groups vs a subset), so "live-editable like `SimTuning`" has a concrete scope? [Clarity, data-model §AiTuning / plan §Project Structure] <!-- Evaluator: Resolved — TR-020(b): ALL AiTuning field groups exposed -->
- [X] CHK018 Is the propagation latency of a live edit defined — whether changed cadences/radii/weights take effect on the next think, the next event, or after `commit_until_tick` expires — so a tuner can interpret what they observe? [Ambiguity, data-model §AiTuning + §AiBrain commit_until_tick] <!-- Evaluator: Resolved — data-model §AiTuning Live-edit semantics: effect at each consumer's next execution (next think/scan/classify); in-flight commit_until_tick not retroactively cancelled -->
- [X] CHK019 Is the interaction between live-edited archetype thresholds and the `Changed<ShipStats>`-only recompute rule specified — does editing a threshold reclassify existing ships, or do stale cached archetypes persist until stats change? [Consistency, data-model §V-5 vs §AiTuning Archetype group] <!-- Evaluator: Resolved — data-model V-5 + §AiTuning + plan AiTuning row: a threshold edit triggers deterministic mass re-classification of ALL brains (dev-panel marks brains changed; force_rederive_all pattern); cost = the accepted O(changed) unbatched case -->
- [X] CHK020 Are determinism/authority consequences of live tuning edits addressed — who may edit, whether an edit reaches the authoritative sim in non-loopback play, and that mid-run edits invalidate replay/golden comparability? [Consistency, plan §Constraints / Spec §TR-004] <!-- Evaluator: Resolved — TR-020: edits loopback-only (unavailable/unauthorized networked); golden/bench runs use pinned defaults; mid-run edit invalidates comparability with recorded runs/goldens -->
- [X] CHK021 Is persistence/reset behavior for tuning edits defined (saved to file like existing tuning resources, or session-only with `#[serde(default)]` fallback), so a tuned configuration is reproducible? [Completeness, Spec §Implementation Signals NEW-CONFIG / data-model §AiTuning] <!-- Evaluator: Resolved — data-model §AiTuning: SimTuning pattern — RON dev-settings save/load with #[serde(default)] per-field fallback; tuned configuration reproducible -->
- [X] CHK022 Do the requirements close the tuning feedback loop the research warns about ("without a score-breakdown view, tuning is blind") — i.e., is it stated that a tuner can observe a weight edit's effect on scores/behavior selection, not merely make the edit? [Coverage, research §Utility-FSM Pitfalls + plan §AD-006] <!-- Evaluator: Resolved — TR-020(b) explicitly requires an edit's effect on scores/behavior selection to be observable in the (a) inspection view -->

## Diagnosing Misbehavior & Determinism Drift

- [X] CHK023 Are behavior-transition diagnostics required (from→to state, triggering event or dominant consideration, tick) so a misbehaving ship's decision history can be reconstructed rather than inferred from motion? [Completeness, data-model §Behavior state machine] <!-- Evaluator: Resolved — TR-020(a): bounded recent transition history (from→to, triggering event/dominant consideration, tick), capture feature-gated like score capture -->
- [X] CHK024 The research prescribes "checksum sim state in golden tests to catch drift early" — is that captured as a requirement or testing-strategy entry anywhere, or does drift detection rest solely on end-state bit-comparison? [Consistency, research §AI Determinism Pitfalls vs plan §Testing Strategy] <!-- Evaluator: Covered by spec TR-019 (per-tick state checksums, first-divergent-tick detection) + plan §Testing Strategy integration row -->
- [X] CHK025 SAD names the replay recorder as the primary desync-diagnosis tool, yet all AI state is ephemeral and non-serialized — do the requirements state whether replaying recorded inputs reproduces AI decisions bit-identically (the "re-derivable" claim made testable), or that AI is out of replay scope? [Consistency, data-model §Scope header vs SAD §Observability] <!-- Evaluator: Resolved — spec §Assumptions: AI state re-derivable + decisions deterministic (TR-004) ⇒ replaying recorded inputs reproduces AI decisions exactly; TR-019 per-tick checksum is the desync detector; no AI-state serialization needed for replay -->
- [X] CHK026 Are diagnostics required to distinguish intentional degraded states from bugs — derelict drift, no-perceived-target fallback, dangling-entity prunes, missing-`AiTuning` defaults — each of which is silent by design in the error-handling table? [Coverage, plan §Error Handling Strategy] <!-- Evaluator: Resolved — TR-020(a): active degraded-state cause displayed (derelict/no-power Hold, no-perceived-target role fallback, missing-tuning defaults) plus LinkState flags -->
- [X] CHK027 Is observability of squad lifecycle events required (member-death re-derive, pace-anchor change, squad-of-1 degradation, collapse/expand), so TR-010/TR-008 failures are diagnosable in a running scenario? [Completeness, Spec §TR-008/TR-010 / data-model §Squad lifecycle] <!-- Evaluator: Resolved — TR-020(a): squad lifecycle events (member-death re-derive, pace-anchor change, squad-of-1 degradation, aggregate collapse/expand) included in the transition history -->
- [X] CHK028 Is a thrash-detection signal or threshold defined that distinguishes healthy promote/demote traffic from tier oscillation, beyond the existence of `tier_hysteresis_ticks`? [Measurability, data-model §AoiTier / research §AI LOD Pitfalls] <!-- Evaluator: Resolved — data-model §AoiTier thrash signal: hysteresis bounds the max per-entity transition rate; a sustained rate near that bound flags oscillation, read via the TR-020 promotion/demotion counters -->
- [X] CHK029 Are perception-diagnosis requirements defined — determining WHY a ship lacks a contact (out of range, signature-gated, jammed/severed, network fragmented) — including visibility into current sensor-network components and each ship's `LinkState`? [Completeness, Spec §TR-013/TR-014 / data-model §SensorNetworks] <!-- Evaluator: Resolved — TR-020(a): ContactList + sensor-network component membership + LinkState shown; with AiTuning ranges/sig thresholds visible in (b), no-contact causes are determinable -->
- [X] CHK030 Is there a defined way to observe promotion-boundary continuity (TR-008 "no positional pop") at runtime — e.g., logging glide-vs-expanded position deltas at promotion — or is detection deferred entirely to integration tests? [Measurability, Spec §TR-008 / plan §Risk Mitigation LOD-boundary] <!-- Evaluator: Resolved — plan §Risk Mitigation makes the boundary explicit: no-pop detection = OBJ3-VC2 boundary test + TR-019 checksums; no runtime promotion-delta logging in v1; TR-020 rate counters expose boundary traffic -->

## Dev-Panel Integration

- [X] CHK031 Is the dev-panel scope boundary explicit — the plan names exactly "AiTuning sliders + per-ship utility score-breakdown view"; are other AI debug surfaces (tier overlay, contact/sensor-network visualization, squad-order display) declared in or out of scope rather than left open? [Clarity, plan §Project Structure dev_panel.rs] <!-- Evaluator: Resolved — TR-020: v1 scope is exactly (a) inspection view (includes squad/order display) + (b) tuning editing + (c) metrics readout; world-space overlays (tier coloring, contact/network viz) declared post-E011 -->
- [X] CHK032 Do the requirements identify the data path from sim AI state to the client dev panel (direct component reads in loopback; explicitly unavailable over the network), so no replication work is silently implied? [Completeness, plan §Project Structure vs SAD/PI server-authoritative + Spec §Constraints MP-safe LOD] <!-- Evaluator: Resolved — TR-020 + plan coverage row: direct ECS reads of the embedded server world in loopback; no replication path; unavailable on a networked client -->
- [X] CHK033 Are the costs of debug/observability features themselves bounded — excluded from (or accounted within) the ≤30% AI budget, and absent from the `fleet_stress` measurement and release/headless builds? [Consistency, Spec §TR-017 vs plan §AD-006] <!-- Evaluator: Covered by plan §AD-006 (capture gated off in headless/release bench builds, zero cost in the measured TR-017 path), restated as a MUST in TR-020 -->
- [X] CHK034 Is reuse of the existing `SimTuning`/`MiningTuning` dev-panel slider pattern a requirement (consistent UX/code conventions) or merely a hint, and is that status explicit? [Clarity, plan §Patterns to reuse] <!-- Evaluator: Resolved — TR-020(b): the SimTuning/MiningTuning slider + RON-save pattern is "a requirement, not a hint" -->
- [X] CHK035 Is dev-panel behavior defined for worlds where AI is gated off (`ScenarioActive` absent / no AI ships) — hidden sections vs empty states — so the panel addition cannot perturb the golden/headless configurations? [Edge-Case, Spec §TR-016] <!-- Evaluator: Resolved — TR-020: AI-gated-off worlds render an empty AI section and write nothing; golden/headless suites run without the client entirely -->
