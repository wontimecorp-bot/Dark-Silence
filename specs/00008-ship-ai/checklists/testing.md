# TESTING: Ship AI architecture — tiered autonomous behaviors at scale
**Created**: 2026-06-10 | **Feature**: [spec.md](../spec.md)

## Determinism Test Requirements

- [X] CHK001 Is "bit-identical" defined with a concrete comparison mechanism (which state fields, byte-level vs checksum, comparison cadence) so a determinism test can be written without inventing the criterion? [Clarity, SC-002/TR-016] <!-- Evaluator: Resolved — SC-002 amended + new TR-019: byte-level per-tick state checksum over Position/Velocity/Heading/ShipIntent + AI behavior state -->
- [X] CHK002 Does SC-002's "re-running the brain on identical sim state" specify the reproduction scope being tested — same process re-run, fresh process, fresh world rebuild — and which of these the requirement actually demands? [Clarity, SC-002/TR-004] <!-- Evaluator: Resolved — SC-002 amended: scope = fresh world rebuilt from the same scenario inputs in the same binary (existing determinism-test pattern); cross-binary out of scope -->
- [X] CHK003 Are the equal-score tiebreak rules consistent across artifacts — plan HINT-002 requires tiebreak "by stable entity id" while data-model's Behavior selection says "tiebreak by behavior-enum ordinal" — and is each tiebreak (across behaviors vs across entities/targets) separately specified so each gets its own test? [Consistency, HINT-002 vs data-model §Behavior] <!-- Evaluator: Resolved — two-level rule stated identically in plan HINT-002 and data-model §Behavior: behavior-enum ordinal intra-ship; stable entity id for all cross-entity ties -->
- [X] CHK004 Is there a requirement for determinism tests covering the NEW AI scenarios themselves (an AI-populated world re-run bit-identical), beyond preserving the existing golden trio that contains no AI ships? [Completeness, TR-016/Edge Cases §Determinism boundary] <!-- Evaluator: Resolved — added TR-019 (AI-populated determinism checksum test) to spec.md + plan coverage map -->
- [X] CHK005 Does any requirement mandate the per-tick state checksum that research flags as the way to "catch drift early," or do tests only compare end states? [Completeness, research §AI Determinism] <!-- Evaluator: Resolved — TR-019 mandates per-tick checksums catching drift at the first divergent tick -->
- [X] CHK006 Is "sim-stable id" (the phase-bucket and tiebreak source) defined precisely enough — who assigns it, when, and its stability guarantees — to write a test asserting V-4? [Clarity, V-4/AD-003] <!-- Evaluator: Resolved — data-model V-4 amended: monotonic spawn-order counter assigned at entity creation by the deterministic spawn path, never reused, identical across re-runs -->
- [X] CHK007 Is the strict-f32 scoring constraint (no fast-math, no libm transcendentals in scores) expressed as a verifiable requirement (testable rule or lint), not just a design note? [Testability, plan §Constraints/research §AI Determinism] <!-- Evaluator: Resolved — TR-004 amended: CI lint/grep over sim::ai scoring code + TR-019 checksum suite as enforcement -->
- [X] CHK008 Does TR-016 enumerate the exact test suites composing the "golden trio" gate (file paths / CI invocations) so the bit-identical gate is unambiguous for tasks? [Clarity, TR-016/plan §Testing Strategy] <!-- Evaluator: Resolved — TR-016 amended: crates/server/tests/determinism.rs, demo_enemies_smoke.rs, harness.rs + botkit/, via cargo test -p server -->

## Objective Validation-Criteria Coverage (OBJ1–7)

- [X] CHK009 Is formation "without oscillation" (OBJ1-VC2) quantified — max slot-position deviation, settle time, or bounded heading chatter — so the test has a pass threshold? [Testability, OBJ1-VC2] <!-- Evaluator: Resolved — OBJ1-VC2 amended: ≤10 s settle, slot error ≤10% of slot spacing, no turn-intent sign flips on consecutive thinks -->
- [X] CHK010 Is the "≈0 decision cost" claim for calm ships (OBJ2-VC2, TR-005) given a measurable assertion (e.g., zero think invocations between events/cadence ticks) rather than a cost adjective? [Testability, OBJ2-VC2/TR-005] <!-- Evaluator: Resolved — OBJ2-VC2 amended: think-invocation counter, zero evaluations between cadence ticks absent events (plus TR-018 calm-fleet bucket) -->
- [X] CHK011 Are "different archetype tactics" (OBJ2-VC3, SC-004) defined with observable, assertable behavior differences (e.g., kiter holds range above X, brawler closes below Y) instead of archetype labels alone? [Testability, OBJ2-VC3/SC-004] <!-- Evaluator: Resolved — OBJ2-VC3 amended: range-band occupancy vs named AiTuning archetype thresholds; non-overlapping median engagement ranges -->
- [X] CHK012 Is "decision work scales with squad count, not N" (OBJ3-VC1) paired with a stated measurement method (think-invocation counters, per-tier counts) a test can assert? [Testability, OBJ3-VC1/TR-009] <!-- Evaluator: Resolved — OBJ3-VC1 amended: per-tier think-invocation counters; doubling N at fixed squad count leaves decision count ~flat -->
- [X] CHK013 Does OBJ3-VC1's "tick stays within the 30Hz budget for the target N" acknowledge that the target N is only defined after the TR-017 bench — and is the test-ordering dependency (bench first, then assertion) stated? [Consistency, OBJ3-VC1/TR-017] <!-- Evaluator: Covered by spec OBJ3-VC1 + TR-017/TR-018 — gate asserted at the pinned baseline N=2000; absolute target N explicitly a bench OUTPUT, not this criterion's gate -->
- [X] CHK014 Is OBJ4-VC1's "destroying the target" time-bounded (max ticks/sim-seconds) so the engagement test can terminate deterministically rather than run open-ended? [Testability, OBJ4-VC1] <!-- Evaluator: Resolved — OBJ4-VC1 amended: ≤3600 ticks (2 sim-minutes @30Hz), fail on timeout -->
- [X] CHK015 Are the ram/no-ram decision thresholds (near-dead, "much weaker," self-margin) tied to the named `AiTuning` fields with default values, so OBJ4-VC2's two scenarios are constructible as fixtures? [Testability, OBJ4-VC2/TR-012/data-model §AiTuning] <!-- Evaluator: Resolved — TR-012 names the fields; data-model AiTuning Ramming row pins defaults (0.25 / 2.0 / 0.5×top-speed) -->
- [X] CHK016 Does a requirement distinctly cover signature gating at the FAR/coarse scan tier (not just near-tier sensing), since the far scan doubles as the promotion trigger and a gap there breaks both OBJ5 and OBJ3? [Completeness, TR-013/V-8/SC-005] <!-- Evaluator: Covered by TR-013 ("far = coarse signature-aware scan... respecting signature at every tier"), data-model V-8, and OBJ5-VC1 ("signature-aware near AND far") -->
- [X] CHK017 Is "returning to patrol after" (OBJ6-VC1) given a defined trigger — threat destroyed, perception-lost, or out of envelope — so the resume condition is testable? [Clarity, OBJ6-VC1/data-model §Behavior transitions] <!-- Evaluator: Covered by data-model §Behavior transitions — Engage→Patrol on target lost (despawn OR perception-lost) → resume ScenarioRole.goal -->
- [X] CHK018 Is the ambush "transition together" criterion (OBJ6-VC2) quantified (same tick? within K ticks?) for a coordinated-trigger assertion? [Testability, OBJ6-VC2] <!-- Evaluator: Resolved — OBJ6-VC2 amended: trigger event forces same-tick re-evaluation; ALL assigned ships transition on the same tick -->
- [X] CHK019 Is "covers the region" (OBJ7-VC2) given a coverage metric (fraction of region cells visited / sensor-swept within a time budget) the S&D test can measure? [Testability, OBJ7-VC2] <!-- Evaluator: Resolved — OBJ7-VC2 amended: ≥90% of the region's coarse interest-tier cells sensor-swept within the scenario time budget -->
- [X] CHK020 Does the plan's integration-test scope cover ALL objective validation criteria — it lists waypoint/formation/engage/derelict/jam-fallback but omits patrol-resume, ambush, hostile-aggregate mutual promotion, scout-disengage, and S&D sweep scenarios (OBJ3-VC3, OBJ6, OBJ7)? [Completeness, plan §Testing Strategy vs OBJ3/OBJ6/OBJ7] <!-- Evaluator: Resolved — plan Integration row now enumerates scenario tests for EVERY spec validation criterion incl. patrol-resume, ambush, mutual promotion, scout disengage, S&D sweep, LOD round-trip -->
- [X] CHK021 Is posture interaction (`DefensiveOnly` + fired-upon, `HoldFire`) covered by any validation criterion or test requirement, given it gates the OBJ6 transitions? [Completeness, data-model §ScenarioRole/OBJ6] <!-- Evaluator: Resolved — posture-gate scenarios (HoldFire never fires; DefensiveOnly engages only after fired-upon) added to the plan Integration row -->
- [X] CHK022 Are the fused-picture semantics (dedupe by target, newest `last_seen_tick` wins, stable sort) stated as testable acceptance points for the OBJ5 network tests, including a contact-disagreement case? [Testability, SC-005/data-model §SensorNetworks] <!-- Evaluator: Resolved — plan Unit row now names the contact-disagreement fusion case (two members, same target, different last_seen_tick → newest wins, stable sort) -->


## LOD / No-Pop / Expand–Collapse Testability

- [X] CHK023 Is "no positional pop / continuous position" (TR-008, SC-003) quantified — exact bit-equality with the glide-extrapolated position at the promote tick, or an epsilon — so the boundary test has a defined comparator? [Testability, TR-008/SC-003] <!-- Evaluator: Resolved — TR-008 amended: bit-exact equality with the glide-extrapolated position; only deviation = bounded validity-nudge ε, AND not inside geometry -->
- [X] CHK024 Do AD-001 ("members stay live entities, no spawn churn") and data-model's dormant rule ("members despawned/parked") agree on the aggregate representation — the expand test asserts identity/fit preservation differently in each case? [Consistency, AD-001 vs data-model §AoiTier] <!-- Evaluator: Covered — data-model §AoiTier + Squad lifecycle realigned to AD-001 (members stay live entities that systems SKIP; promotion re-enables, no spawn churn) -->
- [X] CHK025 Is the promotion-time "deterministic validity nudge" (plan error handling) specified enough to test, and is it reconciled with the no-pop continuity requirement it appears to contradict? [Consistency, plan §Error Handling vs TR-008] <!-- Evaluator: Resolved — nudge defined in TR-008 + plan Error Handling: deterministic, bounded de-penetration along the REVERSE glide direction, applied at promotion before the first full-physics tick; test = within ε of glide position AND not inside geometry -->
- [X] CHK026 Is the re-collapse direction (player leaves) covered by an explicit test requirement, including hysteresis behavior at the tier boundary (no promote/demote thrash within `tier_hysteresis_ticks`)? [Completeness, TR-008/data-model §AoiTier] <!-- Evaluator: Resolved — plan Integration row now names the LOD no-pop promote/demote ROUND-TRIP (OBJ3-VC2); hysteresis (default 30 ticks) covered by data-model §AoiTier + plan LOD classify/hysteresis unit tests -->
- [X] CHK027 Is OBJ3-VC3 (two hostile aggregates mutually promote, no combat while dormant) testable WITHOUT any player entity present, and does a requirement state both the promotion case and the pass-without-combat negative case as separate assertions? [Testability, OBJ3-VC3/Clarification Q1] <!-- Evaluator: Covered by OBJ3-VC3 — "far from any player" with the far-scan trigger (Q1, no player needed); both the mutual-promotion case and the pass-without-combat negative case are stated -->
- [X] CHK028 Is a determinism requirement stated for the expand step itself — re-seeded member positions identical across re-runs of the same approach — distinct from mere continuity? [Completeness, TR-008/Edge Cases §LOD] <!-- Evaluator: Resolved — TR-019 requires the AI determinism scenario to include an aggregate expand/collapse round-trip, re-run bit-identical via per-tick checksums -->


## Edge-Case Test Coverage

- [X] CHK029 Is the derelict/no-power outcome defined as assertable observations (zero `ShipIntent`, no fire, drift per existing rule) and is "must not thrash" given a testable meaning? [Testability, Edge Cases §derelict/plan §Error Handling] <!-- Evaluator: Resolved — plan Error Handling derelict row: "no thrash" = behavior pinned at Hold, ZERO transitions, zero-valued ShipIntent every tick the condition persists -->
- [X] CHK030 Is the no-perceived-target fallback deterministic per role — which of patrol/hold/scout applies when — so the test expects one specific behavior rather than "a sane default"? [Clarity, Edge Cases §no-target/plan §Error Handling] <!-- Evaluator: Resolved — plan Error Handling no-target row: deterministic mapping (ScenarioRole.goal → resume it; Patrol→route, Ambush/Defend→hold at anchor; Scout/Sweep→coverage; else Hold) -->
- [X] CHK031 Are jam-fallback test requirements specified for BOTH `jammed` and `severed` flags, and is the spec's "link-jamming vs sensor-jamming are layered degradations" reconciled with the data-model's single either-flag-excludes treatment? [Consistency, TR-014/OBJ5 vs data-model §LinkState] <!-- Evaluator: Resolved — spec OBJ5 + data-model §LinkState aligned: v1 = link layer only, EITHER flag excludes from connectivity, own local sensing unaffected (sensor-jamming = CAP-007); plan Integration row tests each flag independently -->
- [X] CHK032 Is the anchor-death criterion ("re-derives pace/roles that tick") paired with a definition of "essential member" so the fixture can construct an anchor whose loss must (and a non-anchor whose loss must not) change squad pace? [Clarity, TR-010/OBJ3-VC4] <!-- Evaluator: Resolved — data-model pace_anchor row defines essential (core/non-screen archetype roles) + the must/must-not fixture pair -->
- [X] CHK033 Does the squad-of-1 degradation requirement specify the disposition of residual references (formation_slot, wing parent, squad entity despawn) so the test can assert a clean end state with no dangling refs? [Completeness, TR-010/data-model §Squad lifecycle/V-1] <!-- Evaluator: Resolved — data-model lifecycle Degraded(solo) row: formation_slot→None, order converted, squad entity despawned, wing squad-list pruned same tick (V-1), no dangling refs -->
- [X] CHK034 Is V-1's "pruned the tick their referent despawns, before any brain reads" testable as stated — does a requirement pin the system ordering that a test would verify? [Testability, V-1] <!-- Evaluator: Resolved — V-1 amended: dedicated despawn-sweep system ordered FIRST in the AI system set in add_fixed_step_systems; test despawns a referent and asserts no same-tick AI read -->


## Benchmark as a CI-able Test (TR-017)

- [X] CHK035 Is the ≤30% overhead gate defined with a measurement protocol — N, tick count, warmup, release profile, and tolerance for machine variance — so the bench can act as a non-flaky pass/fail check rather than a manual reading? [Testability, TR-017/SC-003/Clarification Q5] <!-- Evaluator: Covered by TR-018 + plan §TR-017/TR-018 Bench Protocol — N=2000, ≥30 warmup, ≥120-tick window, release build, paired same-machine/session runs, mean gate + p99 budget -->
- [X] CHK036 Is the no-AI baseline's provenance defined (measured fresh in the same run vs a recorded reference number), since the gate is a ratio of the two? [Clarity, TR-017/Q5] <!-- Evaluator: Covered by TR-018 — baseline and --ai runs paired in the same session on the same machine, identical composition; --ai-off byte-identical (uncontaminated baseline) -->
- [X] CHK037 Do the approximations in the acceptance floor ("≤ ~30%", "~2000–4000 ships") get pinned to exact thresholds anywhere, as a gate needs a hard boundary to fail against? [Clarity, Q5/TR-017] <!-- Evaluator: Covered by spec Technical Constraints + TR-017 — hard gate >30.0% fails at the pinned N = 2000 (R57 pinned 2-gun config) @30Hz -->
- [X] CHK038 Does any requirement state how the `fleet_stress --ai` example reports its result for automation (machine-readable output / non-zero exit on breach), given examples are not `cargo test` targets? [Completeness, IP-008/plan §Testing Strategy] <!-- Evaluator: Resolved — TR-018 + plan bench-protocol Output bullet: machine-readable report (per-bucket means, p50/p99, overhead %) + non-zero exit on gate/p99 breach -->


## SC-001 Human-Pilot Trajectory Verification

- [X] CHK039 Does SC-001 specify HOW "a human piloting the same intents" is realized in a test (e.g., a recorded/scripted intent stream replayed through the flight model) and the comparison granularity (per-tick position/velocity, byte-level)? [Testability, SC-001/OBJ1-VC1] <!-- Evaluator: Resolved — OBJ1-VC1 + SC-001 amended: recorded ShipIntent sequence injected as if from player input vs AI emitting the same sequence; bit-identical per-tick position/velocity/heading -->
- [X] CHK040 Is SC-001's verified property stated precisely — since AI and humans share one `ShipIntent` code path, "same intents → same trajectory" is true by construction; does the requirement make clear the test's real target (no direct Velocity/Heading mutation anywhere in the AI path, i.e., TR-001/V-6) so the test asserts the meaningful invariant? [Clarity, SC-001/TR-001/V-6] <!-- Evaluator: Resolved — SC-001 restated: the real invariant is shared-code-path + no AI system writes Velocity/Heading/Position on Active/Mid ships (TR-001/V-6) -->

