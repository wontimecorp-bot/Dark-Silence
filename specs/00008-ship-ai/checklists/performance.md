# PERFORMANCE: Ship AI architecture — tiered autonomous behaviors at scale
**Created**: 2026-06-10 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Is the no-AI `fleet_stress` baseline defined precisely enough to be reproducible — scenario composition, exact ship count N, build profile, and machine/core assumptions — so the ≤30% comparison is falsifiable rather than re-derivable per run? [Completeness, TR-017] <!-- Evaluator: Resolved — pinned baseline (R57 pinned 2-gun config, N=2000 @30Hz, release, one shard/core) added to spec TR-017/TR-018 + Constraints and plan §Performance Goals / §Bench Protocol -->
- [X] CHK002 Does SC-003 specify the player configuration (player count and placement, hence the Active/Mid/Dormant tier mix) under which the ≤30% overhead is asserted, given tier distribution dominates AI cost? [Completeness, SC-003] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol pin one authoritative player at the engagement line (worst-case Active-tier mix); secondary runs report Mid/Dormant mixes -->
- [X] CHK003 Are the consequences of failing the ≤30% bench gate defined (block further behavior work, file BUG tasks, re-scope tiers), or is the gate's enforcement point unspecified? [Completeness, TR-017, plan §Risk Mitigation] <!-- Evaluator: Resolved — plan §Bench Protocol "Gate failure consequences" + Risk Mitigation row: >30% blocks P2+ behavior work, files [BUG:ERROR] {TR-017} tasks, forces tier/cadence re-scope -->
- [X] CHK004 Is "AI cost" required to be reported per bucket (think, steering, perception scans, squad brains, LOD classify, coarse-tier build) rather than only as an aggregate delta, so an overrun can be localized? [Completeness, TR-017] <!-- Evaluator: Resolved — per-bucket reporting (incl. coarse-tier build + separately-reported off-screen battles) required by new spec TR-018 + plan §Bench Protocol -->
- [X] CHK005 Are warmup and measurement-window requirements specified for the bench (ticks discarded, sample count, run repetitions), so one-time costs like initial archetype classification and first index build don't skew the overhead figure? [Completeness, IP-008] <!-- Evaluator: Resolved — TR-018: ≥30 warmup ticks discarded, ≥120-tick measured window, paired same-session baseline/--ai runs (R57 methodology) -->
- [X] CHK006 Does any requirement state where the bench-derived absolute concurrent-ship scale target is recorded and which artifact (SC-003 refinement, tuning resource, ADR) consumes it? [Completeness, TR-017] <!-- Evaluator: Resolved — TR-017 + plan §Scale/Scope/§Bench Protocol: target N is a bench OUTPUT recorded in the bench report and written back to plan Performance Goals / SC-003 -->
- [X] CHK007 Are target values or acceptable ranges required for the per-tier think cadences (`think_ticks_active/_mid/_dormant`, fallback bucket count), or are they left as unbounded tuning fields with no requirement-level budget? [Completeness, TR-005, data-model §AiTuning] <!-- Evaluator: Resolved — data-model §AiTuning think-cadence row now pins defaults: active fallback ≈0.5 s (15 ticks), mid ≈0.5 s, dormant 2–5 s (Q4-aligned), bucket count 16 -->
- [X] CHK008 Are perception scan cost bounds defined per tier — what a near every-think scan, a mid ~0.5 s fused squad scan, and a far 2–5 s coarse scan may each query (radius, cells, candidate count)? [Completeness, TR-013, Clarification Q4] <!-- Evaluator: Resolved — data-model §AiTuning Sensors row: near = sensor-range fine-grid radius query; mid = ONE fused query/squad/~0.5 s; far = ≤ ~3×3 coarse cells/2–5 s; max_fused_contacts cap -->
- [X] CHK009 Is the cadence and cost of the sensor-network connectivity flood-fill + fusion rebuild specified (every tick, on change, or per scan cadence), given component membership can span whole fleets? [Completeness, TR-014] <!-- Evaluator: Resolved — data-model §SensorNetworks: rebuild at the mid scan cadence (~0.5 s) + on LinkState/membership events, NOT every tick; O(C log C) fusion capped by max_fused_contacts -->
- [X] CHK010 Is tier hysteresis given a requirement-level value range or rationale (ticks before promote/demote), and is thrash cost at the AOI boundary covered by any validation criterion? [Completeness, TR-008, data-model §AoiTier] <!-- Evaluator: Resolved — data-model §AoiTier: default tier_hysteresis_ticks ≈ 30 (1 s @30Hz) with anti-thrash rationale; covered by the LOD classify/hysteresis unit tests in plan §Testing Strategy -->
- [X] CHK011 Is the coarse interest tier's per-tick build cost stated as a requirement (build-once-read-many is asserted, but is full rebuild every tick required and bounded by entity count)? [Completeness, IP-002, AD-002] <!-- Evaluator: Resolved — plan IP-002 row: both grids rebuilt once/tick, coarse build = O(entities) inserts over few large cells, cost reported as a named bench bucket -->
- [X] CHK012 Is the "graceful degradation, no hard cap" behavior specified — what degrades first and in what order when the tick budget is exceeded (cadence stretch, tier demotion, scan skips)? [Completeness, spec §Technical Constraints] <!-- Evaluator: Resolved — spec §Technical Constraints now fixes the order: (1) stretch fallback cadences (Mid then Active), (2) Mid→Dormant demotion from the AOI edge inward, (3) skip non-event scans; player-adjacent event reactions degrade last -->

## Clarity

- [X] CHK013 Is "≤ ~30%" resolved to a hard pass/fail threshold — does the tilde permit exceeding 30%, and by how much, before the gate fails? [Clarity, SC-003, Clarification Q5] <!-- Evaluator: Resolved — tilde removed: spec Constraints/TR-017/SC-003 + plan now read "≤ 30% (hard gate — >30.0% fails)" -->
- [X] CHK014 Is "sustainable N (~2000–4000 fighting ships/core @30Hz)" pinned to a single concrete N or a defined procedure for deriving it, rather than a 2× range that lets the gate be measured at the friendliest point? [Ambiguity, spec §Technical Constraints, Clarification Q5] <!-- Evaluator: Resolved — pinned to N = 2000 @30Hz (the documented sustainable point of the R57 pinned 2-gun fleet_stress config) in spec Constraints/TR-017 + plan Performance Goals -->
- [X] CHK015 Is it unambiguous whether the coarse interest-tier build cost counts as "AI overhead" in the ≤30% figure or as shared baseline infrastructure (it also serves sensors/sector-sharding seams)? [Ambiguity, TR-017, IP-002] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: coarse-tier build counts INSIDE the AI numerator in v1 (own bucket); reclassify to shared baseline only when a second non-AI consumer ships -->
- [X] CHK016 Does OBJ3's validation criterion "the tick stays within the 30Hz budget for the target N" define "target N" before the bench that sets the target exists, or is the criterion circular at acceptance time? [Ambiguity, OBJ3 VC-1, TR-017] <!-- Evaluator: Resolved — OBJ3 VC-1 reworded: gate is the ≤30% relative budget at the pinned baseline N (falsifiable day one); absolute target N is an OUTPUT of the bench, not the gate -->
- [X] CHK017 Is the STF-001 revisit trigger ("when concurrent-world-combat load becomes real") expressed as a measurable signal (e.g., promoted-battle count or tick-share threshold) rather than an unfalsifiable judgment call? [Ambiguity, STF-001] <!-- Evaluator: Resolved — STF-001 + plan Risk Mitigation: revisit when the separately-reported off-screen promoted-battle bucket sustainedly exceeds 10% of the 33.3 ms tick budget (observable because the bench always reports it) -->
- [X] CHK018 Is a query budget for the coarse tier stated (target cells scanned per AOI/far-scan query), given the rationale that a coarse query on the fine grid "would scan thousands of cells" implies an intended bound? [Clarity, OBJ3 Rationale, AD-002] <!-- Evaluator: Resolved — plan IP-002 + data-model Sensors row: coarse cells sized so one AOI/far-scan query touches ≤ ~3×3 coarse cells -->
- [X] CHK019 Is it clear which tiers run full context-steering (16-slot maps) per ship — Active only, or also Mid-tier members executing squad orders — since "O(1) member execution" and per-member context maps have different costs? [Clarity, AD-004, TR-009] <!-- Evaluator: Resolved — plan AD-004 row: full context maps on Active-tier ships ONLY; Mid members execute via O(1) slot/order-vector steering with the danger mask only (no per-member interest-map build) -->

## Consistency

- [X] CHK020 Are the dual tick budgets reconciled — the constraints name 30Hz (33.3 ms) and 60Hz (16.7 ms), but the gate and goals reference only 30Hz; is the 60Hz case in or out of scope for the ≤30% assertion? [Consistency, spec §Technical Constraints, plan §Performance Goals] <!-- Evaluator: Resolved — spec Constraints + plan Performance Goals: the gate is asserted @30Hz only; the 60Hz figure is informational and out of the ≤30% assertion's scope -->
- [X] CHK021 Is the `--ai` bench mode required to leave the no-AI path byte-identical when off, so adding AI wiring to `fleet_stress` cannot contaminate the baseline it is measured against? [Consistency, IP-008, TR-016] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: with --ai absent, fleet_stress spawns no AI components and runs byte-identical to today's no-AI path -->
- [X] CHK022 Is the AI run required to match the baseline run's composition and combat activity (same N, same fight) so the overhead delta isolates AI cost rather than scenario drift? [Consistency, TR-017] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: paired same-machine/session runs with identical composition and the same pinned facing-lines fight -->
- [X] CHK023 Do AD-001 ("keep members as live entities, systems skip them") and the data-model's dormant note ("members despawned/parked; promotion re-expands members") describe the same dormant representation — and which cost profile (skip-cost per entity vs spawn churn) do requirements assume? [Consistency, AD-001, data-model §AoiTier] <!-- Evaluator: Resolved — data-model §AoiTier + squad-lifecycle row amended to the approved AD-001 decision: members stay live entities that systems SKIP (skip-cost profile, no spawn churn); promotion re-enables, not respawns -->
- [X] CHK024 Is the measurement scope for off-screen promoted battles defined consistently — STF-001 asserts the budget "relative to player-local load" (excluding them) while the plan says the bench "measures promoted-battle cost"; which loads are inside the ≤30% denominator and numerator? [Consistency, STF-001, plan §Risk Mitigation] <!-- Evaluator: Resolved — per the STF-001 resolution the budget is player-local: off-screen promoted battles are EXCLUDED from the ≤30% numerator but MUST be separately measured + reported (spec Constraints/TR-017/SC-003/STF-001 + plan Performance Goals/Risk Mitigation/Bench Protocol now agree) -->
- [X] CHK025 Is it specified which consumers may query the coarse tier vs the fine grid (AOI/far scans coarse; near sensor queries fine), so requirements prevent reintroducing the fine-grid scan cost the coarse tier exists to avoid? [Consistency, IP-002] <!-- Evaluator: Resolved — plan IP-002 row: AOI/LOD + far coarse scans query ONLY the coarse tier (never a coarse-radius query against the fine grid); near/mid sensor radius queries + collision use the fine grid -->

## Measurability & Testability

- [X] CHK026 Is the "per-tick time" statistic defined (mean, p95, or worst tick over a stated window), so spiky AI cost — promotion bursts, scan ticks — cannot pass on a favorable average? [Measurability, TR-017, SC-003] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: gate statistic = mean over the ≥120-tick window; p50/p99 reported and p99 must hold the 33.3 ms budget (spikes can't hide in the mean) -->
- [X] CHK027 Are environment controls for the bench specified — release build, CPU/core pinning or a stated normalization for "ships/core" — so the figure is comparable across runs and machines? [Measurability, plan §Testing Strategy, plan §Performance Goals] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: release build, one shard/core (the "ships/core" normalization), baseline/--ai runs paired on the same machine/session -->
- [X] CHK028 Is the "≈0 decision cost for calm ships" claim given a verifiable form (an idle-ship per-tick cost ceiling, or a bench case comparing calm vs fighting fleets)? [Measurability, TR-005] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol require a calm-fleet bench case (no hostiles) where the think bucket ≈ 0, verifying event-driven idle savings against the fighting run -->
- [X] CHK029 Is "decision work scales with squad count, not ship count" testable as written — is the per-member O(1) execution cost bounded so it provably cannot dominate as N grows, and is a scaling measurement (N vs squads sweep) required? [Measurability, TR-009, SC-003] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol require a fixed-N squad-size sweep (e.g., N=2000 in squads of 10 vs 40): decision bucket tracks squad count, per-member execution stays ~constant (AD-004 bounds it to slot/order-vector steering) -->
- [X] CHK030 Does the bench requirement state what the AI fleet is doing during measurement (sustained combat vs patrol), ensuring the measured case is the expensive one where events fire continuously and event-driven savings vanish? [Measurability, TR-005, TR-017] <!-- Evaluator: Resolved — TR-018 + plan §Bench Protocol: gate run = the pinned sustained facing-lines combat (events fire continuously); calm/patrol is the secondary comparison case -->

## Coverage & Edge Cases

- [X] CHK031 Is the event-storm case addressed — in a big battle, hit/new-contact events can fire for most ships every tick, collapsing event-driven scheduling to per-tick thinking; is that worst case bounded or accepted by a requirement? [Coverage, TR-005] <!-- Evaluator: Resolved — data-model §Behavior: events COALESCE to at most ONE think per brain per tick (last_think_tick guard); that bounded worst case IS the bench's measured sustained-combat gate case -->
- [X] CHK032 Is phase-bucket burst alignment covered — can many ships share a bucket (or buckets align with squad scan ticks) so one tick absorbs a think burst, and is a spread/bound requirement stated? [Coverage, AD-003, data-model §AiBrain.phase_bucket] <!-- Evaluator: Resolved — data-model §AiTuning think-cadence row: stable-id hash spreads brains ~uniformly across 16 buckets (≈ N/16 per tick); squad scan phases offset by squad-id hash so buckets don't align into burst ticks -->
- [X] CHK033 Is mass archetype re-classification bounded — a spawn wave or fleet-wide refit flips `Changed<ShipStats>` on many ships in one tick; is that spike amortized or accepted? [Coverage, TR-006] <!-- Evaluator: Resolved — data-model §AiBrain.archetype: accepted unbatched — classification is a pure O(1) threshold function of ShipStats (no scans/queries), so a wave costs O(changed) trivial work -->
- [X] CHK034 Is fused-picture growth bounded — per-component dedupe and stable sort over contacts in a galaxy-scale battle; is there a contact cap or cost bound per network component? [Coverage, TR-014, data-model §SensorNetworks] <!-- Evaluator: Resolved — data-model: fused picture capped by new AiTuning.max_fused_contacts (newest/highest-signature kept, deterministic cut); fusion O(C log C) per rebuild at the ~0.5 s cadence, not per tick -->
- [X] CHK035 Is the one-tick cost of aggregate expansion bounded — re-seeding a large squad's members at continuous positions on player approach (or mutual hostile promotion of two fleets) within a single tick's budget? [Coverage, TR-008, Clarification Q1] <!-- Evaluator: Resolved — data-model squad-lifecycle row: expansion = O(members) re-enable of live entities (AD-001, no spawn churn), bounded by max_squad_size/wing size; mutual promotion = O(both memberships); bursts surface in the bench p99 (TR-018 gates p99) -->
- [X] CHK036 Is the far-tier hostile-detection cost bounded — coarse scans across many dormant aggregates approach pairwise checks; do requirements bound this by the coarse grid rather than O(aggregates²)? [Coverage, Clarification Q1, TR-013] <!-- Evaluator: Resolved — plan IP-002 row: far hostile detection = per-aggregate coarse-neighborhood query (≤ ~3×3 cells) at the 2–5 s cadence — O(aggregates × neighbors-in-range), grid-bounded, never O(aggregates²) -->
- [X] CHK037 Is the dev-panel per-ship score-breakdown view required to be dev-only with zero cost in release/bench builds, so the tuning aid cannot leak into the measured overhead? [Coverage, AD-006] <!-- Evaluator: Resolved — plan AD-006 row: view lives in the client-crate dev-panel only; score-breakdown capture is gated off in headless/release bench builds (zero cost in the measured TR-017 path) -->
